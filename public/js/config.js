/**
 * public/js/config.js
 * ─────────────────────────────────────────────────────────────
 * BEFORE DEPLOYING: replace both values below with your real
 * Supabase project credentials.
 *
 * Find them at: supabase.com → your project → Settings → API
 *
 *   SUPABASE_URL   — "Project URL"     e.g. https://abcdef.supabase.co
 *   SUPABASE_ANON  — "anon / public"   starts with eyJ...
 *
 * These values are intentionally in frontend code — the anon key
 * is public-safe and required for Supabase Auth token refresh.
 * NEVER put your SERVICE_ROLE key here.
 * ─────────────────────────────────────────────────────────────
 */
window.QR_SUPABASE_URL  = 'REPLACE_WITH_SUPABASE_URL';
window.QR_SUPABASE_ANON = 'REPLACE_WITH_SUPABASE_ANON_KEY';
