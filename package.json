{
  "name": "qr-studio",
  "version": "2.0.0",
  "description": "QR Studio — AI Promo Design SaaS",
  "private": true,
  "engines": { "node": ">=18.x" },
  "scripts": {
    "dev":    "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
